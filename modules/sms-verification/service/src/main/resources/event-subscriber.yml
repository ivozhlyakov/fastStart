asyncapi: 2.0.0
info:
  title: Template application
  version: '1.01.01'
channels:
  sms-verification-delivered:
    subscribe:
      message:
        $ref: '#/components/messages/smsVerificationDeliveredMessage'
components:
  messages:
    smsVerificationDeliveredMessage:
      payload:
        $ref: '#/components/schemas/smsVerificationDeliveredMessage'
  schemas:
    smsVerificationDeliveredMessage:
      type: object   
      properties:
        code:
          type: string